# LOAD_VECTOR = "LV"
# LOAD_RASTER = "LR"
# LOAD_CSV = "LC"

FUNCTIONS_CHOICES = (
    # (LOAD_VECTOR, "load vector, .shp files"),
    # (LOAD_RASTER, "load raster, .tif files"),
    # (LOAD_CSV, "load csv files")
)
